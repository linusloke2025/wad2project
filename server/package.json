{
  "name": "wad2-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "IS216 event-coordination app — Express + MongoDB backend",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "seed": "node src/scripts/seed.js",
    "test:unit": "vitest run --pool=threads"
  },
  "engines": {
    "node": "^22.18.0 || >=24.12.0"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.10.1"
  }
}
